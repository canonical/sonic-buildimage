Index: sonic-platform-modules-ragile/common/modules/linux-5.10/wb_i2c_mux_pca954x.c
===================================================================
--- sonic-platform-modules-ragile.orig/common/modules/linux-5.10/wb_i2c_mux_pca954x.c
+++ sonic-platform-modules-ragile/common/modules/linux-5.10/wb_i2c_mux_pca954x.c
@@ -861,7 +861,7 @@ static irqreturn_t pca954x_irq_handler(i
 
     for (i = 0; i < data->chip->nchans; i++) {
         if (ret & BIT(PCA954X_IRQ_OFFSET + i)) {
-            child_irq = irq_linear_revmap(data->irq, i);
+            child_irq = irq_find_mapping(data->irq, i);
             handle_nested_irq(child_irq);
             handled++;
         }
Index: sonic-platform-modules-ragile/common/modules/wb_eeprom_93xx46.c
===================================================================
--- sonic-platform-modules-ragile.orig/common/modules/wb_eeprom_93xx46.c
+++ sonic-platform-modules-ragile/common/modules/wb_eeprom_93xx46.c
@@ -396,8 +396,6 @@ static int eeprom_93xx46_probe_dt(struct
 	struct device_node *np = spi->dev.of_node;
 	struct eeprom_93xx46_platform_data *pd;
 	u32 tmp;
-	int gpio;
-	enum of_gpio_flags of_flags;
 	int ret;
 
 	pd = devm_kzalloc(&spi->dev, sizeof(*pd), GFP_KERNEL);
@@ -422,21 +420,15 @@ static int eeprom_93xx46_probe_dt(struct
 	if (of_property_read_bool(np, "read-only"))
 		pd->flags |= EE_READONLY;
 
-	gpio = of_get_named_gpio_flags(np, "select-gpios", 0, &of_flags);
-	if (gpio_is_valid(gpio)) {
-		unsigned long flags =
-			of_flags == OF_GPIO_ACTIVE_LOW ? GPIOF_ACTIVE_LOW : 0;
-
-		ret = devm_gpio_request_one(&spi->dev, gpio, flags,
-					    "eeprom_93xx46_select");
-		if (ret)
-			return ret;
-
-		pd->select = gpio_to_desc(gpio);
+	pd->select = devm_gpiod_get_optional(&spi->dev, "select", GPIOD_OUT_LOW);
+	if (IS_ERR(pd->select)) {
+		ret = PTR_ERR(pd->select);
+		pd->select = NULL;
+		return ret;
+	}
+	if (pd->select) {
 		pd->prepare = select_assert;
 		pd->finish = select_deassert;
-
-		gpiod_direction_output(pd->select, 0);
 	}
 
     if (of_id) {
Index: sonic-platform-modules-ragile/common/modules/wb_wdt.c
===================================================================
--- sonic-platform-modules-ragile.orig/common/modules/wb_wdt.c
+++ sonic-platform-modules-ragile/common/modules/wb_wdt.c
@@ -7,6 +7,7 @@
 #include <linux/delay.h>
 #include <linux/module.h>
 #include <linux/of_gpio.h>
+#include <linux/gpio/consumer.h>
 #include <linux/platform_device.h>
 #include <linux/watchdog.h>
 #include <linux/hrtimer.h>
@@ -657,7 +658,7 @@ static int gpio_wdt_init(wb_wdt_priv_t *
 {
     struct device *dev;
     gpio_wdt_info_t *gpio_wdt;
-    enum of_gpio_flags flags;
+    struct gpio_desc *desc;
     uint32_t f = 0;
     int ret;
 
@@ -665,29 +666,36 @@ static int gpio_wdt_init(wb_wdt_priv_t *
     gpio_wdt = &priv->gpio_wdt;
 
     if (dev->of_node) {
-        gpio_wdt->gpio = of_get_gpio_flags(dev->of_node, 0, &flags);
+        desc = devm_gpiod_get_index(dev, NULL, 0, GPIOD_ASIS);
+        if (IS_ERR(desc)) {
+            dev_err(dev, "gpio is invalid.\n");
+            return PTR_ERR(desc);
+        }
+        gpio_wdt->gpio = desc_to_gpio(desc);
+        gpio_wdt->active_low = gpiod_is_active_low(desc);
     } else {
         gpio_wdt->gpio = wb_wdt_device->wdt_config_mode.gpio_wdt.gpio;
-        flags = wb_wdt_device->wdt_config_mode.gpio_wdt.flags;
+        gpio_wdt->active_low = false;
     }
     if (!gpio_is_valid(gpio_wdt->gpio)) {
         dev_err(dev, "gpio is invalid.\n");
         return gpio_wdt->gpio;
     }
 
-    gpio_wdt->active_low = flags & OF_GPIO_ACTIVE_LOW;
-
     if(priv->hw_algo == HW_ALGO_TOGGLE) {
         f = GPIOF_IN;
     } else {
         f = gpio_wdt->active_low ? GPIOF_OUT_INIT_HIGH : GPIOF_OUT_INIT_LOW;
     }
 
-    ret = devm_gpio_request_one(dev, gpio_wdt->gpio, f,
-                dev_name(dev));
-    if (ret) {
-        dev_err(dev, "devm_gpio_request_one failed.\n");
-        return ret;
+    /* In the DT path the line was already acquired by devm_gpiod_get_index;
+     * only request it explicitly for the non-DT (platform_data) path. */
+    if (!dev->of_node) {
+        ret = devm_gpio_request_one(dev, gpio_wdt->gpio, f, dev_name(dev));
+        if (ret) {
+            dev_err(dev, "devm_gpio_request_one failed.\n");
+            return ret;
+        }
     }
 
     gpio_wdt->state = gpio_wdt->active_low;
Index: sonic-platform-modules-ragile/common/modules/wb_wdt.h
===================================================================
--- sonic-platform-modules-ragile.orig/common/modules/wb_wdt.h
+++ sonic-platform-modules-ragile/common/modules/wb_wdt.h
@@ -7,7 +7,6 @@
 
 typedef struct gpio_wdt_info_s {
     int       gpio;
-    enum of_gpio_flags  flags;
     bool      active_low;
     bool      state;
 }gpio_wdt_info_t;
