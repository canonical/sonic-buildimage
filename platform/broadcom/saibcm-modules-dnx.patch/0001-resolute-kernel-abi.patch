Index: saibcm-modules-dnx/debian/control
===================================================================
--- saibcm-modules-dnx.orig/debian/control
+++ saibcm-modules-dnx/debian/control
@@ -10,5 +10,5 @@ Standards-Version: 3.9.3
 Package: opennsl-modules
 Architecture: amd64
 Section: main
-Depends: linux-image-6.12.41+deb13-sonic-amd64-unsigned
+Depends: linux-image-7.0.0-1002-sonic
 Description: kernel modules for broadcom SAI
Index: saibcm-modules-dnx/debian/rules
===================================================================
--- saibcm-modules-dnx.orig/debian/rules
+++ saibcm-modules-dnx/debian/rules
@@ -34,9 +34,11 @@ sname:=opennsl
 PACKAGE=opennsl-modules
 # modifieable for experiments or debugging m-a
 MA_DIR ?= /usr/share/modass
-KVER := $(word 1,$(subst -, ,$(KVERSION)))
-KVER_ARCH := $(KVER)-sonic-amd64
-KVER_COMMON := $(KVER)-common-sonic
+# Resolute linux-sonic 7.0.0-1002-sonic: ABI string has no arch suffix,
+# and common headers dir uses 'linux-sonic-headers-' prefix.
+# Use exported KERNEL_VERSION / KERNEL_ABISUFFIX instead of parsing KVERSION.
+KVER_ARCH := $(KVERSION)
+KVER_COMMON := $(KERNEL_VERSION)$(KERNEL_ABISUFFIX)
 
 # load generic variable handling
 -include $(MA_DIR)/include/generic.make
@@ -65,11 +67,11 @@ kdist_clean: clean
 	dh_testdir
 	dh_clean
 	SDK=$(realpath .) LINUX_UAPI_SPLIT=1 DEBIAN_LINUX_HEADER=1 BUILD_KNET_CB=1 \
-	    KERNDIR=/usr/src/linux-headers-$(KVER_COMMON) \
+	    KERNDIR=/usr/src/linux-sonic-headers-$(KVER_COMMON) \
 	    KERNEL_SRC=/usr/src/linux-headers-$(KVER_ARCH) \
 	    $(MAKE) -C systems/linux/user/x86-smp_generic_64-2_6 clean
 	SDK=$(realpath .) BUILD_KNET_CB=1 \
-	    KDIR=/usr/src/linux-headers-$(KVER_COMMON) \
+	    KDIR=/usr/src/linux-sonic-headers-$(KVER_COMMON) \
 	    $(MAKE) -C sdklt/ clean
 #	rm -f driver/*.o driver/*.ko
 #
@@ -91,30 +93,30 @@ build-arch-stamp:
 	cd /; sudo mkdir -p /lib/modules/$(KVER_ARCH)
 	cd /; sudo rm /lib/modules/$(KVER_ARCH)/build
 	cd /; sudo rm /lib/modules/$(KVER_ARCH)/source
-	cd /; sudo ln -s /usr/src/linux-headers-$(KVER_COMMON)/ /lib/modules/$(KVER_ARCH)/source
+	cd /; sudo ln -s /usr/src/linux-sonic-headers-$(KVER_COMMON)/ /lib/modules/$(KVER_ARCH)/source
 	cd /; sudo ln -s /usr/src/linux-headers-$(KVER_ARCH)/ /lib/modules/$(KVER_ARCH)/build
-	if [ ! -e /usr/src/linux-headers-$(KVER_COMMON)/include/generated ]; then \
-		sudo ln -s /usr/src/linux-headers-$(KVER_ARCH)/include/generated/ /usr/src/linux-headers-$(KVER_COMMON)/include/generated; \
+	if [ ! -e /usr/src/linux-sonic-headers-$(KVER_COMMON)/include/generated ]; then \
+		sudo ln -s /usr/src/linux-headers-$(KVER_ARCH)/include/generated/ /usr/src/linux-sonic-headers-$(KVER_COMMON)/include/generated; \
 	fi
-	if [ ! -e /usr/src/linux-headers-$(KVER_COMMON)/arch/x86/include/generated ]; then \
-		sudo ln -s /usr/src/linux-headers-$(KVER_ARCH)/arch/x86/include/generated/ /usr/src/linux-headers-$(KVER_COMMON)/arch/x86/include/generated; \
+	if [ ! -e /usr/src/linux-sonic-headers-$(KVER_COMMON)/arch/x86/include/generated ]; then \
+		sudo ln -s /usr/src/linux-headers-$(KVER_ARCH)/arch/x86/include/generated/ /usr/src/linux-sonic-headers-$(KVER_COMMON)/arch/x86/include/generated; \
 	fi
-	if [ ! -e /usr/src/linux-headers-$(KVER_COMMON)/arch/x86/module.lds ]; then \
-		sudo ln -s /usr/src/linux-headers-$(KVER_ARCH)/arch/x86/module.lds /usr/src/linux-headers-$(KVER_COMMON)/arch/x86/module.lds; \
+	if [ ! -e /usr/src/linux-sonic-headers-$(KVER_COMMON)/arch/x86/module.lds ]; then \
+		sudo ln -s /usr/src/linux-headers-$(KVER_ARCH)/arch/x86/module.lds /usr/src/linux-sonic-headers-$(KVER_COMMON)/arch/x86/module.lds; \
 	fi
-	if [ ! -e /usr/src/linux-headers-$(KVER_COMMON)/include/config ]; then \
-		sudo ln -s /usr/src/linux-headers-$(KVER_ARCH)/include/config/ /usr/src/linux-headers-$(KVER_COMMON)/include/config; \
+	if [ ! -e /usr/src/linux-sonic-headers-$(KVER_COMMON)/include/config ]; then \
+		sudo ln -s /usr/src/linux-headers-$(KVER_ARCH)/include/config/ /usr/src/linux-sonic-headers-$(KVER_COMMON)/include/config; \
 	fi
-	sudo cp /usr/src/linux-headers-$(KVER_ARCH)/Module.symvers /usr/src/linux-headers-$(KVER_COMMON)/Module.symvers
+	sudo cp /usr/src/linux-headers-$(KVER_ARCH)/Module.symvers /usr/src/linux-sonic-headers-$(KVER_COMMON)/Module.symvers
 
 	# Add here command to compile/build the package.
 	SDK=$(realpath .) LINUX_UAPI_SPLIT=1 DEBIAN_LINUX_HEADER=1 BUILD_KNET_CB=1 \
-	    KERNDIR=/usr/src/linux-headers-$(KVER_COMMON) \
+	    KERNDIR=/usr/src/linux-sonic-headers-$(KVER_COMMON) \
 	    KERNEL_SRC=/usr/src/linux-headers-$(KVER_ARCH) \
 	    $(MAKE) -C systems/linux/user/x86-smp_generic_64-2_6
 
 	SDK=$(realpath .) BUILD_KNET_CB=1 \
-	    KDIR=/usr/src/linux-headers-$(KVER_COMMON) \
+	    KDIR=/usr/src/linux-sonic-headers-$(KVER_COMMON) \
 	    $(MAKE) -C sdklt/ kmod
 
 	touch $@
@@ -141,12 +143,12 @@ clean:
 
 	# Add here commands to clean up after the build process.
 	SDK=$(realpath .) LINUX_UAPI_SPLIT=1 DEBIAN_LINUX_HEADER=1 BUILD_KNET_CB=1 \
-	    KERNDIR=/usr/src/linux-headers-$(KVER_COMMON) \
+	    KERNDIR=/usr/src/linux-sonic-headers-$(KVER_COMMON) \
 	    KERNEL_SRC=/usr/src/linux-headers-$(KVER_ARCH) \
 	    $(MAKE) -C systems/linux/user/x86-smp_generic_64-2_6 clean
 
 	SDK=$(realpath .) BUILD_KNET_CB=1 \
-	    KDIR=/usr/src/linux-headers-$(KVER_COMMON) \
+	    KDIR=/usr/src/linux-sonic-headers-$(KVER_COMMON) \
 	    $(MAKE) -C sdklt/ clean
 
 	dh_clean
