Index: sonic-platform-modules-tencent/common/modules/rg_gpio_d1500.c
===================================================================
--- sonic-platform-modules-tencent.orig/common/modules/rg_gpio_d1500.c
+++ sonic-platform-modules-tencent/common/modules/rg_gpio_d1500.c
@@ -122,7 +122,7 @@ static int rg_gpio_direction_in(struct g
     return 0;
 }
 
-static void rg_gpio_set(struct gpio_chip *gc,
+static int rg_gpio_set(struct gpio_chip *gc,
                 unsigned gpio_num, int val)
 {
     u32 data;
@@ -160,7 +160,7 @@ static void rg_gpio_set(struct gpio_chip
     }
     spin_unlock_irqrestore(&sio_lock, flags);
 
-    return;
+    return 0;
 }
 
 static int rg_gpio_direction_out(struct gpio_chip *gc,
Index: sonic-platform-modules-tencent/common/modules/rg_wdt.c
===================================================================
--- sonic-platform-modules-tencent.orig/common/modules/rg_wdt.c
+++ sonic-platform-modules-tencent/common/modules/rg_wdt.c
@@ -326,7 +326,7 @@ static enum hrtimer_restart hrtimer_hwpi
     rg_wdt_priv_t *priv = container_of(timer, rg_wdt_priv_t, hrtimer);
 
     wdt_hwping(priv);
-    hrtimer_forward(timer, timer->base->get_time(), priv->m_kt);
+    hrtimer_forward(timer, hrtimer_cb_get_time(timer), priv->m_kt);
     return HRTIMER_RESTART;
 }
 
@@ -468,8 +468,7 @@ static int hrtimer_cfg(rg_wdt_priv_t *pr
     }
 
     priv->m_kt = ktime_set(hrtimer_s, hrtimer_ns);
-    hrtimer_init(hrtimer, CLOCK_MONOTONIC, HRTIMER_MODE_REL);
-    hrtimer->function = hrtimer_hwping;
+    hrtimer_setup(hrtimer, hrtimer_hwping, CLOCK_MONOTONIC, HRTIMER_MODE_REL);
     hrtimer_start(hrtimer, priv->m_kt, HRTIMER_MODE_REL);
 
     ret = rg_wdt_enable_ctrl(priv, WDT_ON);
