name: Rock target

on:
  workflow_dispatch:
    inputs:
      branch_name:
        description: 'branch name'
        required: true
        default: 'feature_noble_build'
        type: string
  push:
    branches: [ feature_noble_build, feat/rock ]

jobs:
  testflinger-broadcom:
    runs-on: [self-hosted , testflinger]
    steps:
      - uses: actions/checkout@v4

      - name: Generate job YAML
        uses: cuchi/jinja2-action@v1.3.0
        with:
          template: ${{ github.workspace }}/.github/testflinger/broadcom_install_rock.yaml.j2
          output_file: tmp_broadcom_install_rock.yaml
          strict: true
          variables: |
            branch=${{ github.head_ref || github.ref_name }}

      - name: Check job yaml
        run: |
          # Copy to solve the file permission issue
          cp tmp_broadcom_install_rock.yaml broadcom_install_rock.yaml
          ls -al broadcom_install_rock.yaml
          cat broadcom_install_rock.yaml

      - name: Submit job
        id: submit-job
        uses: canonical/testflinger/.github/actions/submit@main
        with:
          poll: true
          job-path: broadcom_install_rock.yaml

      - name: Display results
        id: get-results
        run: |
          testflinger results ${{ steps.submit-job.outputs.id }} > results.json
          cat results.json

      - name: Check build job status
        run: |
          set -e
          testflinger results ${{ steps.submit-job.outputs.id }} > results.json
          status=$(jq '.test_status' results.json)
          if ! [[ "$status" =~ ^[0-9]+$ ]]; then
            echo "Invalid or missing test_status in results.json"
            exit 1
          fi
          if [ "$status" -ne 0 ]; then
              echo "testflinger task failed with status $status"
              jq -r '.test_output' results.json
              exit 1
          else
              echo "testflinger task succeeded"
          fi
